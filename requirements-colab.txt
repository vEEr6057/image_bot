# Requirements for Google Colab GPU deployment
# PyTorch with CUDA is pre-installed on Colab, so we don't include it here
# Using versions compatible with Colab's pre-installed packages

python-telegram-bot==20.8
python-dotenv==1.0.0
opencv-python-headless>=4.9.0.80
Pillow>=10.1.0
requests>=2.32.4
apscheduler==3.11.1

# Official Real-ESRGAN with proper tile blending
basicsr>=1.4.2
facexlib>=0.3.0
gfpgan>=1.3.8
realesrgan>=0.3.0
